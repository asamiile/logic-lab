# Separation and Seek

```bash
uv run python simulation/separation_and_seek/separation_and_seek.py
```

- Vehicles seek the mouse position while separating from nearby vehicles.
- Press `s` to save a screenshot to `simulation/separation_and_seek/screenshots/`.
