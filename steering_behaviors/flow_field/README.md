# Flow Field

```bash
uv run python simulation/flow_field/flow_field.py
```

- Vehicles follow a Perlin-noise flow field.
- Press space to toggle the debug vectors.
- Click to generate a new flow field.
- Press `s` to save a screenshot to `simulation/flow_field/screenshots/`.
