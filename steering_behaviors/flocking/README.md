# Flocking

```bash
uv run python simulation/flocking/flocking.py
```

- Boids flock using separation, alignment, and cohesion.
- Drag the mouse to add boids.
- Press `s` to save a screenshot to `simulation/flocking/screenshots/`.
