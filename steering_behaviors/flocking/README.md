# Flocking

```bash
uv run python steering_behaviors/flocking/flocking.py
```

- Boids flock using separation, alignment, and cohesion.
- Drag the mouse to add boids.
- Press `s` to save a screenshot to `steering_behaviors/flocking/screenshots/`.
