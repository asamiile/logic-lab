# Pursuit Behavior

```bash
uv run python steering_behaviors/pursuit_behavior/pursuit_behavior.py
```

- A vehicle (triangle) pursues a moving target (circle) by predicting its future position.
- On collision the target resets to a random position and the pursuer returns to center.
- Press `s` to save a screenshot to `steering_behaviors/pursuit_behavior/screenshots/`.
