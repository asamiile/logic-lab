# Path Following

```bash
uv run python simulation/path_following/path_following.py
```

- Two vehicles follow a randomly generated multi-segment path.
- Press space to toggle the debug lines.
- Click to generate a new path.
- Press `s` to save a screenshot to `simulation/path_following/screenshots/`.
