# Arrive

```bash
uv run python steering_behaviors/arrive/arrive.py
```

- A vehicle follows the mouse position and slows down as it arrives.
- Press `s` or click to save a screenshot to `steering_behaviors/arrive/screenshots/`.
