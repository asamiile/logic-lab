# Wander Behavior

```bash
uv run python simulation/wander_behavior/wander_behavior.py
```

- A vehicle wanders autonomously by steering toward a point on a projected circle ahead of it.
- Click to toggle the debug visualization (wander circle and target point).
- Press `s` to save a screenshot to `simulation/wander_behavior/screenshots/`.
