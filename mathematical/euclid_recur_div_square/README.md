# Euclid Recur Div Square

Recursively divides a square and its generated rectangles. Click to randomize `numA`, `numB`, and `thr`.

```bash
uv run python mathematical_art/euclid_recur_div_square/euclid_recur_div_square.py
```

Press `s` to save a screenshot.
