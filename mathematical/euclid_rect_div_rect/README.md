# Euclid Rect Div Rect

Divides a rectangle into squares, then divides each square into similar rectangles.

```bash
uv run python mathematical/euclid_rect_div_rect/euclid_rect_div_rect.py
```

Press `s` to save a screenshot.
