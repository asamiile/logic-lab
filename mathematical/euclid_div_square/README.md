# Euclid Div Square

Divides a square into `10:6` rectangles using Euclidean-style repeated subdivision.

```bash
uv run python mathematical/euclid_div_square/euclid_div_square.py
```

Press `s` to save a screenshot.
