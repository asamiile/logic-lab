# Continued Fraction Div Square

Divides a square into `10:6` rectangles using repeated continued-fraction-style subdivision.

```bash
uv run python mathematical/continued_fraction_div_square/continued_fraction_div_square.py
```

Press `s` to save a screenshot.

