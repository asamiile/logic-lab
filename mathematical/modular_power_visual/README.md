# Modular Power Visual

Visualizes a power table modulo `37` with hue and circle size.

```bash
uv run python mathematical/modular_power_visual/modular_power_visual.py
```

Press `s` to save a screenshot.

