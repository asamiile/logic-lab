# Log Spiral Zoom

Draws a zoomable logarithmic spiral. Move the mouse horizontally to change the zoom scale.

```bash
uv run python mathematical/log_spiral_zoom/log_spiral_zoom.py
```

Press `s` to save a screenshot.

