# Asteroids

```bash
uv run python research/asteroids/asteroids.py
```

- A spaceship turns with the arrow keys, thrusts while `z` is held, and wraps around the canvas edges.
- Press `s` to save a screenshot to `research/asteroids/screenshots/`.
