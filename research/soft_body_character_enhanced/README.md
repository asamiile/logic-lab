# Soft Body Character Enhanced

```bash
uv run python simulation/soft_body_character_enhanced/soft_body_character_enhanced.py
```

- A soft-body character built from particles and dense spring connections.
- Hold mouse to drag the first body particle.
- Press `Space` to toggle spring visualization.
- Press `s` to save a screenshot to `simulation/soft_body_character_enhanced/screenshots/`.
