# Soft Body Character Copy

```bash
uv run python simulation/soft_body_character_copy/soft_body_character_copy.py
```

- A soft body built from 6 particles and multiple springs.
- Hold mouse to drag the first particle.
- Press `s` to save a screenshot to `simulation/soft_body_character_copy/screenshots/`.
