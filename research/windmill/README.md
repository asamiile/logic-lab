# Windmill

```bash
uv run python simulation/windmill/windmill.py
```

- Particles fall from the top and strike a pivoting windmill blade, causing it to spin.
- The blade is pinned at its center and rotates freely around that point.
- Press `s` to save a screenshot to `simulation/windmill/screenshots/`.
