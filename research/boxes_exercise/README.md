# Boxes Exercise

```bash
uv run python simulation/boxes_exercise/boxes_exercise.py
```

- Click and drag to spawn boxes that fall under gravity and stack on the ground.
- Press `s` to save a screenshot to `simulation/boxes_exercise/screenshots/`.
