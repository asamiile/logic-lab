# Mouse Constraint

```bash
uv run python simulation/mouse_constraint/mouse_constraint.py
```

- Click and drag boxes to move them around the enclosed arena.
- A spring constraint connects your mouse to the grabbed body, allowing dynamic interaction.
- Press `s` to save a screenshot to `simulation/mouse_constraint/screenshots/`.
