# Bridge

```bash
uv run python simulation/bridge/bridge.py
```

- A chain of linked particles forms a bridge with fixed endpoints.
- Random boxes fall from above and collide with the bridge.
- Press `s` to save a screenshot to `simulation/bridge/screenshots/`.
