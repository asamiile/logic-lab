# Collision Events

```bash
uv run python simulation/collision_events/collision_events.py
```

- Particles fall and collide with each other and the floor.
- On particle-particle collision, both particles change color.
- Press `s` to save a screenshot to `simulation/collision_events/screenshots/`.
