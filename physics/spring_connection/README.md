# Spring Connection

```bash
uv run python simulation/spring_connection/spring_connection.py
```

- A bob is connected to an anchor by a spring force and constrained length.
- Drag the bob with the mouse to stretch the spring.
- Press `s` to save a screenshot to `simulation/spring_connection/screenshots/`.
