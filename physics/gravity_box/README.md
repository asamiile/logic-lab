# Gravity Box

```bash
uv run python simulation/gravity_box/gravity_box.py
```

- A rigid box falls under gravity, bounces off the ground, and spins.
- pymunk is used as the physics engine (equivalent to Matter.js in the original).
- Press `s` to save a screenshot to `simulation/gravity_box/screenshots/`.
