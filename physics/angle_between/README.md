# Angle Between Vectors

```bash
uv run python simulation/angle_between/angle_between.py
```

- Move the mouse to rotate the blue vector; the angle between it and the x-axis is displayed.
- Press `s` to save a screenshot to `simulation/angle_between/screenshots/`.
