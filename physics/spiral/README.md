# Spiral

```bash
uv run python simulation/spiral/spiral.py
```

- A polar coordinate spirals outward as both angle and radius increase over time.
- Press `s` to save a screenshot to `simulation/spiral/screenshots/`.
