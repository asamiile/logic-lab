# Fluid Resistance

```bash
uv run python simulation/fluid_resistance/fluid_resistance.py
```

- Click mouse to reset with new movers.
- Press `s` to save a screenshot to `simulation/fluid_resistance/screenshots/`.
