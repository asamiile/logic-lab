# rotate_baton

```bash
uv run python simulation/rotate_baton/rotate_baton.py
```

- A baton rotates around the center of the canvas.
- Press `s` to save a screenshot to `simulation/rotate_baton/screenshots/`.
