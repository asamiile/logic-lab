# Paint Splatter

```bash
uv run python simulation/paint_splatter/paint_splatter.py
```

- Press `Space` to clear the canvas.
- Press `s` to save a screenshot to `simulation/paint_splatter/screenshots/`.
