# Multiple Emitters

```bash
uv run python simulation/multiple_emitters/multiple_emitters.py
```

- Click to add particle emitters at the mouse position.
- Each emitter creates one particle per frame and removes dead particles.
- Press `s` to save a screenshot to `simulation/multiple_emitters/screenshots/`.
