# Accelerate and Decelerate

```bash
uv run python simulation/accelerate_and_decelerate/accelerate_and_decelerate.py
```

- Press `↑` to accelerate the train.
- Press `↓` to decelerate the train.
- Press `s` to save a screenshot to `simulation/accelerate_and_decelerate/screenshots/`.
