# Single Particle

```bash
uv run python simulation/single_particle/single_particle.py
```

- A single particle falls under gravity, fades out, and respawns when its lifespan ends.
- Press `s` to save a screenshot to `simulation/single_particle/screenshots/`.
