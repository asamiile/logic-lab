# CA 2dim

Draws a two-dimensional cellular automaton using the von Neumann neighborhood modulo `mod`. Click to randomize the modulus.

```bash
uv run python cellular_automata/ca_2dim/ca_2dim.py
```

Press `s` to save a screenshot.

