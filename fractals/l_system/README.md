# L-System

```bash
uv run python simulation/l_system/l_system.py
```

- L-system expansion with rule: `F -> FF+[+F-F-F]-[-F+F+F]`.
- Turtle interpreter draws the generated sentence after 4 generations.
- Press `s` to save a screenshot to `simulation/l_system/screenshots/`.
