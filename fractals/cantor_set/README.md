# Cantor Set

```bash
uv run python fractals/cantor_set/cantor_set.py
```

- Renders the Cantor set recursively with line segments.
- Each step draws the left and right thirds on the next row.
- Press `s` to save a screenshot to `fractals/cantor_set/screenshots/`.
