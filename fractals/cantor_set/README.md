# Cantor Set

```bash
uv run python simulation/cantor_set/cantor_set.py
```

- Renders the Cantor set recursively with line segments.
- Each step draws the left and right thirds on the next row.
- Press `s` to save a screenshot to `simulation/cantor_set/screenshots/`.
