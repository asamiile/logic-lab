# Stochastic Tree

```bash
uv run python simulation/stochastic_tree/stochastic_tree.py
```

- Recursive tree with random branch count and random branch angles.
- Regenerates a new random tree each second (`frame_rate(1)`).
- Press `s` to save a screenshot to `simulation/stochastic_tree/screenshots/`.
