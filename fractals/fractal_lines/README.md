# Fractal Lines

```bash
uv run python simulation/fractal_lines/fractal_lines.py
```

- Recursive fractal line drawing that subdivides lines into perpendicular branches.
- The pattern is generated based on line orientation (horizontal or vertical).
- Press `s` to save a screenshot to `simulation/fractal_lines/screenshots/`.
