# P6M Pattern

Generates a hexagonal lattice pattern with 60-degree rotation and reflection symmetry, translated from `GenerativeArtWithMath/Ch12_Pattern/P6M`.

```bash
uv run python tiling_patterns/p6m_pattern/p6m_pattern.py
```

Click to regenerate the random filled motif. Press `s` to save a screenshot.
