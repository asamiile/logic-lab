# Recursive Pentagon

Recursively subdivides a regular pentagon into smaller pentagons, translated from `GenerativeArtWithMath/Ch14_Aperiodic/RecurPentagon`.

```bash
uv run python tiling_patterns/recur_pentagon/recur_pentagon.py
```

Click to advance one subdivision. Press `r` to reset and `s` to save a screenshot.
