# Textile Generator

Interactive textile matrix generator. Click cells in matrices `A`, `B`, or `C` to toggle values and update the generated pattern.

```bash
uv run python tiling_patterns/textile_generator/textile_generator.py
```

Press `s` to save a screenshot.

