# P6 Pattern

Generates a hexagonal lattice pattern with 60-degree rotational symmetry, translated from `GenerativeArtWithMath/Ch12_Pattern/P6`.

```bash
uv run python tiling_patterns/p6_pattern/p6_pattern.py
```

Click to regenerate the random Bezier curve. Press `s` to save a screenshot.
