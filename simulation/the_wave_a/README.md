# The Wave A

```bash
uv run python simulation/the_wave_a/the_wave_a.py
```

- A static sine wave is drawn across a 200 by 200 canvas.
- Press `s` to save a screenshot to `simulation/the_wave_a/screenshots/`.
