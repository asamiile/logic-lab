# Boxes

```bash
uv run python simulation/boxes/boxes.py
```

- Hold mouse button to spawn boxes.
- Boxes fall with gravity and are removed after leaving the screen.
- Press `s` to save a screenshot to `simulation/boxes/screenshots/`.
