# Forces With Arbitrary Angular Motion

```bash
uv run python simulation/forces_with_arbitrary_angular_motion/forces_with_arbitrary_angular_motion.py
```

- Movers are attracted to the center while their angular motion responds to acceleration.
- Press `s` to save a screenshot to `simulation/forces_with_arbitrary_angular_motion/screenshots/`.
