# Recursion Circles

```bash
uv run python simulation/recursion_circles/recursion_circles.py
```

- Recursive circle fractal with four child circles per parent.
- Recursion stops when radius becomes 16 or less.
- Press `s` to save a screenshot to `simulation/recursion_circles/screenshots/`.
