# Recursion 2

```bash
uv run python simulation/recursion_2/recursion_2.py
```

- Recursive circle fractal where each circle spawns left and right children.
- Stops recursion when radius becomes small.
- Press `s` to save a screenshot to `simulation/recursion_2/screenshots/`.
