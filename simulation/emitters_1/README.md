# Emitters 1

```bash
uv run python simulation/emitters_1/emitters_1.py
```

- Multiple emitters each create one particle per frame.
- Click to add a new emitter at the mouse position.
- Press `s` to save a screenshot to `simulation/emitters_1/screenshots/`.
