# Buoyancy Balloon

```bash
uv run python simulation/buoyancy_balloon/buoyancy_balloon.py
```

- A balloon floats upward with helium, drifts with Perlin noise wind, bounces off the ceiling, and wraps around the sides.
- Press `s` to save a screenshot to `simulation/buoyancy_balloon/screenshots/`.
