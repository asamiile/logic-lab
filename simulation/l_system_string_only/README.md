# L-System String Only

```bash
uv run python simulation/l_system_string_only/l_system_string_only.py
```

- String-only L-system generations rendered as text.
- Rules: `A -> AB`, `B -> A`, starting axiom `A`.
- Press `s` to save a screenshot to `simulation/l_system_string_only/screenshots/`.
