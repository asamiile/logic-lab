# Polygon Shapes

```bash
uv run python simulation/polygon_shapes/polygon_shapes.py
```

- Custom convex polygons spawn at the top and fall onto two static platforms.
- Shapes that fall off the screen are removed from the physics world.
- Press `s` to save a screenshot to `simulation/polygon_shapes/screenshots/`.
