# Emitters 2

```bash
uv run python simulation/emitters_2/emitters_2.py
```

- Four emitters create particles from different starting positions.
- Click to add a new emitter at the mouse position.
- Press `s` to save a screenshot to `simulation/emitters_2/screenshots/`.
