# Array Particles

```bash
uv run python simulation/array_particles/array_particles.py
```

- A new particle is added each frame, and dead particles are removed from the array.
- Press `s` to save a screenshot to `simulation/array_particles/screenshots/`.
