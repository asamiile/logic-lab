# Random Walk Right

## Example 0.3: A Walker That Tends to Move to the Right

```bash
uv run python simulation/random_walk_right/random_walk_right.py
```

Press `s` to save a screenshot to `simulation/random_walk_right/screenshots/`.
