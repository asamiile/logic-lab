# Quadtree Part 1

```bash
uv run python simulation/quadtree_part_1/quadtree_part_1.py
```

- Move the mouse to query points within a 100×100 region (shown in red).
- Press `s` to save a screenshot to `simulation/quadtree_part_1/screenshots/`.
