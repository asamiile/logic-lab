# Soft String

```bash
uv run python simulation/soft_string/soft_string.py
```

- A soft string built from chained Verlet particles and springs.
- The first particle is fixed; drag the tail by holding the mouse.
- Press `s` to save a screenshot to `simulation/soft_string/screenshots/`.
