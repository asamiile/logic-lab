# Boxes and Boundaries

```bash
uv run python simulation/boxes_and_boundaries/boxes_and_boundaries.py
```

- Boxes spawn randomly at the top center and fall under gravity onto two angled static platforms.
- Boxes that fall off the screen are removed from the physics world.
- Press `s` to save a screenshot to `simulation/boxes_and_boundaries/screenshots/`.
