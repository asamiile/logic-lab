# Matter.js Pendulum

```bash
uv run python simulation/matter_js_pendulum/matter_js_pendulum.py
```

- A pendulum with a static anchor and a swinging bob connected by a rigid arm.
- Press `s` to save a screenshot to `simulation/matter_js_pendulum/screenshots/`.
