# Rep-Tiles

Self-similar tiling where each tile subdivides into scaled copies of itself.

```bash
uv run python src/logic_lab/tiling_patterns/rep_tiles/rep_tiles.py
```

Press `s` to save a screenshot.

Recursive subdivision showing rep-tile structure at multiple scales.
