# Recursive Triangle

Recursively subdivides thin and fat golden triangles, translated from `GenerativeArtWithMath/Ch14_Aperiodic/RecurTriangle`.

```bash
uv run python src/logic_lab/tiling_patterns/recur_triangle/recur_triangle.py
```

Click to advance one subdivision. Press `r` to reset and `s` to save a screenshot.
