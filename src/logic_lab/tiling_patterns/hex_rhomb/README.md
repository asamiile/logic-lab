# Hex Rhomb

Builds each hexagonal tile from three rhombi, translated from `GenerativeArtWithMath/Ch13_Periodic/HexRhomb`.

```bash
uv run python tiling_patterns/hex_rhomb/hex_rhomb.py
```

Click to regenerate colors. Press `s` to save a screenshot.
