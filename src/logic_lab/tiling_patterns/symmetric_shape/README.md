# Symmetric Shape

Generates a random Bezier wedge and repeats it with dihedral symmetry. Click to add another random symmetric shape.

```bash
uv run python tiling_patterns/symmetric_shape/symmetric_shape.py
```

Press `s` to save a screenshot.

