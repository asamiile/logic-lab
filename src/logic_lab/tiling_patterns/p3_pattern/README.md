# P3 Pattern

Generates a hexagonal lattice pattern with 120-degree rotational symmetry, translated from `GenerativeArtWithMath/Ch12_Pattern/P3`.

```bash
uv run python tiling_patterns/p3_pattern/p3_pattern.py
```

Click to regenerate the random Bezier motif. Press `s` to save a screenshot.
