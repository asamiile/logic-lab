# rotate_baton

```bash
uv run python physics/rotate_baton/rotate_baton.py
```

- A baton rotates around the center of the canvas.
- Press `s` to save a screenshot to `physics/rotate_baton/screenshots/`.
