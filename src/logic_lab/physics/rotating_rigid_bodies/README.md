# Rotating Rigid Bodies

```bash
uv run python src/logic_lab/physics/rotating_rigid_bodies/rotating_rigid_bodies.py
```

Convex polygon bodies move, rotate, collide with SAT, and bounce off the canvas.
It extends circle-only collision into a more engine-like rigid-body demo.

- Click to add a rectangle.
- Press `s` to save a screenshot.
