# Static Wave

```bash
uv run python physics/static_wave/static_wave.py
```

- A row of circles is positioned with sine values to form a static wave.
- Press `s` to save a screenshot to `physics/static_wave/screenshots/`.
