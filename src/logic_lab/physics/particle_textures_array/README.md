# Particle Textures Array

```bash
uv run python src/logic_lab/physics/particle_textures_array/particle_textures_array.py
```

- Emits particles from the mouse position.
- Each particle randomly uses one of three texture images generated in code.
- Press `s` to save a screenshot to `physics/particle_textures_array/screenshots/`.
