# Particle Shatter

```bash
uv run python physics/particle_shatter/particle_shatter.py
```

- A block made from square particles shatters when clicked.
- Press `s` to save a screenshot to `physics/particle_shatter/screenshots/`.
