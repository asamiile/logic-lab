# Spatial Hash Broad Phase

```bash
uv run python src/logic_lab/physics/spatial_hash_broad_phase/spatial_hash_broad_phase.py
```

A uniform spatial hash reduces pair checks before narrow-phase circle collision.
The grid is visible so the broad-phase grouping can be inspected directly.

- Press `s` to save a screenshot.
