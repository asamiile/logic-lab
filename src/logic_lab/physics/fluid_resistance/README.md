# Fluid Resistance

```bash
uv run python physics/fluid_resistance/fluid_resistance.py
```

- Click mouse to reset with new movers.
- Press `s` to save a screenshot to `physics/fluid_resistance/screenshots/`.
