# Verlet Constraints

```bash
uv run python src/logic_lab/physics/verlet_constraints/verlet_constraints.py
```

A rope-like constraint chain built with Verlet integration and distance
constraints. This is the core technique behind cloth, rope, simple soft bodies,
and position-based dynamics.

- Drag the mouse to pull the chain end.
- Press `s` to save a screenshot.
