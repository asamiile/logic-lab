# Accept-Reject Distribution

```bash
uv run python src/logic_lab/physics/accept_reject_distribution/accept_reject_distribution.py
```

Press `s` to save a screenshot to `physics/accept_reject_distribution/screenshots/`.
