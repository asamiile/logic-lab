# Bouncing Ball (No Vectors)

```bash
uv run python src/logic_lab/physics/bouncing_ball_no_vectors/bouncing_ball_no_vectors.py
```

Press `s` to save a screenshot to `physics/bouncing_ball_no_vectors/screenshots/`.
