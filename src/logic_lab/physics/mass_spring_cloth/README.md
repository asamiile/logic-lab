# Mass Spring Cloth

```bash
uv run python src/logic_lab/physics/mass_spring_cloth/mass_spring_cloth.py
```

A cloth mesh built from Verlet points and distance constraints. It demonstrates
how a mass-spring surface can become a flexible material without a full external
physics engine.

- Hold the mouse near the cloth to push points away.
- Press `s` to save a screenshot.
