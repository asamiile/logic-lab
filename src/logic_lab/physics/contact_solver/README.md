# Contact Solver

```bash
uv run python src/logic_lab/physics/contact_solver/contact_solver.py
```

A simple iterative contact solver stacks axis-aligned boxes under gravity. It
demonstrates positional correction, contact friction, and resting contacts.

- Click to add another box.
- Press `s` to save a screenshot.
