# Fluid Particles

```bash
uv run python src/logic_lab/physics/fluid_particles/fluid_particles.py
```

A lightweight fluid-like particle system. Neighboring particles push away from
local density and share velocity through viscosity, creating a compact SPH-like
motion study without a heavyweight solver.

- Hold the mouse to pour in more particles.
- Press `s` to save a screenshot.
