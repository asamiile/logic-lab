# Box Collision

```bash
uv run python src/logic_lab/physics/box_collision/box_collision.py
```

AABB broad-phase boxes and OBB narrow-phase collision are shown together. The
mouse-controlled box turns red when the rotated rectangles overlap.

- Move the mouse to move one box.
- Press `s` to save a screenshot.
