# Soft Body Lattice

```bash
uv run python src/logic_lab/physics/soft_body_lattice/soft_body_lattice.py
```

A spring-like lattice behaves as a simple soft body. Structural and diagonal
constraints preserve shape while allowing jelly-like deformation.

- Hold the mouse near the body to push it.
- Press `s` to save a screenshot.
