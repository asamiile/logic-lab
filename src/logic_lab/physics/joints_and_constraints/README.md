# Joints And Constraints

```bash
uv run python src/logic_lab/physics/joints_and_constraints/joints_and_constraints.py
```

Distance joints form a bridge-like truss with fixed anchors. Secondary diagonal
constraints keep the structure from collapsing, similar to simple physics-engine
joint networks.

- Hold the mouse to pull the center joint.
- Press `s` to save a screenshot.
