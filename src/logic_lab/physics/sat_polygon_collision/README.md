# SAT Polygon Collision

```bash
uv run python src/logic_lab/physics/sat_polygon_collision/sat_polygon_collision.py
```

Separating Axis Theorem collision detection for convex polygons. The red vector
shows the minimum translation direction when two polygons overlap.

- Move the mouse to move the pentagon.
- Press `s` to save a screenshot.
