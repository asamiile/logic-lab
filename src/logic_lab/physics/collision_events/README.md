# Collision Events

```bash
uv run python src/logic_lab/physics/collision_events/collision_events.py
```

Circle collisions emit contact events containing a point, normal, and impulse.
The sketch renders velocity vectors, expanding contact rings, and normal lines,
making collision response easier to debug or use as a visual motif.

- Press `s` to save a screenshot.
