# Raycast Queries

```bash
uv run python src/logic_lab/physics/raycast_queries/raycast_queries.py
```

A ray is cast against rotating convex polygons and reports the nearest hit,
including contact point, distance, and surface normal.

- Move the mouse to aim the ray.
- Press `s` to save a screenshot.
