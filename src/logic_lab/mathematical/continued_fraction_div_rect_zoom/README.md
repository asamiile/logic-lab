# Continued Fraction Div Rect Zoom

Animated zoom into the `sqrt(2):1` rectangle subdivision. Move the mouse horizontally to change the zoom scale.

```bash
uv run python src/logic_lab/mathematical/continued_fraction_div_rect_zoom/continued_fraction_div_rect_zoom.py
```

Press `s` to save a screenshot.

