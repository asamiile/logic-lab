# Superellipse (Lamé Curve)

Parametric curves interpolating between squares and circles by varying the exponent n.

```bash
uv run python src/logic_lab/mathematical/superellipse/superellipse.py
```

Press `s` to save a screenshot.

The exponent animates smoothly, morphing shape between extremes.
