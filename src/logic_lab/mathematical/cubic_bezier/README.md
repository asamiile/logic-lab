# Cubic Bezier

Visualizes construction of a cubic Bezier curve with interpolated line segments.

```bash
uv run python src/logic_lab/mathematical/cubic_bezier/cubic_bezier.py
```

Press `s` to save a screenshot.
