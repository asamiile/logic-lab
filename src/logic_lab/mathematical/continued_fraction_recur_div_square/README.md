# Continued Fraction Recur Div Square

Recursively divides a square and generated rectangles. Click to randomize `numA`, `numB`, and `thr`.

```bash
uv run python src/logic_lab/mathematical/continued_fraction_recur_div_square/continued_fraction_recur_div_square.py
```

Press `s` to save a screenshot.

