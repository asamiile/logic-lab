# Hatching Patterns

Parallel lines with varying density encode brightness through sine-based shading.

```bash
uv run python src/logic_lab/mathematical/hatching_patterns/hatching_patterns.py
```

Press `s` to save a screenshot.

Animated dual-layer crosshatch creates depth and luminosity variations.
