# Shape Morphing

Polygon vertices smoothly interpolate between different geometric shapes through continuous animation.

```bash
uv run python src/logic_lab/mathematical/shape_morphing/shape_morphing.py
```

Press `s` to save a screenshot.

Triangle-to-hexagon transformation highlights the beauty of shape transition.
