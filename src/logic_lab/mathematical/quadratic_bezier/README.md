# Quadratic Bezier

Visualizes construction of a quadratic Bezier curve with interpolated line segments.

```bash
uv run python src/logic_lab/mathematical/quadratic_bezier/quadratic_bezier.py
```

Press `s` to save a screenshot.

