# Euclid Div Rect

Divides a `10:6` rectangle into squares using the Euclidean algorithm.

```bash
uv run python src/logic_lab/mathematical/euclid_div_rect/euclid_div_rect.py
```

Press `s` to save a screenshot.
