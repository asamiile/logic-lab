# Conic Sections

Parametric drawing of ellipses, parabolas, and hyperbolas with animated color transitions and shape transformations.

```bash
uv run python src/logic_lab/mathematical/conic_sections/conic_sections.py
```

Press `s` to save a screenshot.

The shapes animate continuously, changing size and color over time.
