# Continued Fraction Div Rect

Divides a rectangle with a `sqrt(2):1` ratio into squares.

```bash
uv run python src/logic_lab/mathematical/continued_fraction_div_rect/continued_fraction_div_rect.py
```

Press `s` to save a screenshot.

