# Modular Table Visual

Visualizes an addition table modulo `7` with hue and circle size.

```bash
uv run python src/logic_lab/mathematical/modular_table_visual/modular_table_visual.py
```

Press `s` to save a screenshot.
