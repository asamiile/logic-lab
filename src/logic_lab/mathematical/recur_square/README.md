# Recur Square

Draws nested squares by repeatedly moving each vertex toward the next vertex. Click to randomize the gap.

```bash
uv run python src/logic_lab/mathematical/recur_square/recur_square.py
```

Press `s` to save a screenshot.
