# Julia Set

Complex plane iteration with animated c-parameter producing morphing Julia fractal boundaries.

```bash
uv run python src/logic_lab/fractals/julia_set/julia_set.py
```

Press `s` to save a screenshot.

c-parameter animates continuously, transforming the fractal shape.
