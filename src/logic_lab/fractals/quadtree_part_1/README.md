# Quadtree Part 1

```bash
uv run python src/logic_lab/fractals/quadtree_part_1/quadtree_part_1.py
```

- Move the mouse to query points within a 100×100 region (shown in red).
- Press `s` to save a screenshot to `fractals/quadtree_part_1/screenshots/`.
