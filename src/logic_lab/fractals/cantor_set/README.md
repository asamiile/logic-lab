# Cantor Set

Classical fractal generated by recursive line segment removal.

```bash
uv run python src/logic_lab/fractals/cantor_set/cantor_set.py
```

Press `s` to save a screenshot.

Hierarchical levels of Cantor set construction visualized vertically.
