# Mandelbrot Set

High-quality escape-time fractal rendering of the Mandelbrot set with color mapping.

```bash
uv run python src/logic_lab/fractals/mandelbrot_set/mandelbrot_set.py
```

Press `s` to save a screenshot.

Static fractal with iteration-based coloring.
