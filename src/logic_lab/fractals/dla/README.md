# Diffusion Limited Aggregation (DLA)

Random walkers accumulating into fractal-like growth patterns.

```bash
uv run python src/logic_lab/fractals/dla/dla.py
```

Press `s` to save a screenshot.

Continuous particle aggregation; stats displayed.
