# Stochastic Tree

```bash
uv run python src/logic_lab/fractals/stochastic_tree/stochastic_tree.py
```

- Recursive tree with random branch count and random branch angles.
- Regenerates a new random tree each second (`frame_rate(1)`).
- Press `s` to save a screenshot to `fractals/stochastic_tree/screenshots/`.
