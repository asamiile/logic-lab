# Koch Curve

```bash
uv run python src/logic_lab/fractals/koch_curve/koch_curve.py
```

- Generates a Koch curve by replacing each segment with four new segments.
- Applies the generation step 5 times, then renders the final fractal.
- Press `s` to save a screenshot to `fractals/koch_curve/screenshots/`.
