# Recursion Circles

```bash
uv run python src/logic_lab/fractals/recursion_circles/recursion_circles.py
```

- Recursive circle fractal with four child circles per parent.
- Recursion stops when radius becomes 16 or less.
- Press `s` to save a screenshot to `fractals/recursion_circles/screenshots/`.
