# Collision Events

```bash
uv run python src/logic_lab/research/collision_events/collision_events.py
```

- Particles fall and collide with each other and the floor.
- On particle-particle collision, both particles change color.
- Press `s` to save a screenshot to `research/collision_events/screenshots/`.
