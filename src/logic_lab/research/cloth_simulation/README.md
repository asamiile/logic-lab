# Cloth Simulation

```bash
uv run python src/logic_lab/research/cloth_simulation/cloth_simulation.py
```

- A cloth is modeled as a grid of Verlet particles connected by springs.
- Some particles along the top edge are pinned, and gravity pulls the cloth downward.
- Press `s` to save a screenshot to `research/cloth_simulation/screenshots/`.
