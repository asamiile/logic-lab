# Soft String

```bash
uv run python src/logic_lab/research/soft_string/soft_string.py
```

- A soft string built from chained Verlet particles and springs.
- The first particle is fixed; drag the tail by holding the mouse.
- Press `s` to save a screenshot to `research/soft_string/screenshots/`.
