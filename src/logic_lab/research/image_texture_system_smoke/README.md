# Image Texture System Smoke

```bash
uv run python src/logic_lab/research/image_texture_system_smoke/image_texture_system_smoke.py
```

- Smoke particles are drawn with an alpha texture and are pushed by mouse-controlled wind.
- Press `s` to save a screenshot to `research/image_texture_system_smoke/screenshots/`.
