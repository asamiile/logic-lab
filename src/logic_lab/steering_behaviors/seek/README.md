# Seek

```bash
uv run python src/logic_lab/steering_behaviors/seek/seek.py
```

- A vehicle seeks the mouse position using a steering force.
- Press `s` to save a screenshot to `steering_behaviors/seek/screenshots/`.
